name: dsai490_a2
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - pytorch-cuda=11.8
  - numpy
  - pandas
  - matplotlib
  - tqdm
  - pip
  - pip:
      # add model-specific deps here
